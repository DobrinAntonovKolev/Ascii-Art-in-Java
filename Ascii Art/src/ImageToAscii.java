/*import javax.imageio.ImageIO;
import java.awt.image.*;
import java.io.*;
import java.awt.*;

public class ImageToAscii {

    File file;
    BufferedImage image;
    int width;
    int height;
    Color[][] colorGrid;
    int[][] brightnessGrid;
    char[][] asciiGrid;
    Exception exception = new Exception("Invalid File");

    ImageToAscii(String pathName) throws Exception, IOException {
        file = new File(pathName);
        if (!file.exists()) {
            throw exception;
        }
    }
}
*/